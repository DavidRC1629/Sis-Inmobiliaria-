# Hace que JaCoCo ignore los métodos generados por Lombok (getters, setters, constructores, builders)
# Esto sube automáticamente la cobertura de entities al 100%
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
