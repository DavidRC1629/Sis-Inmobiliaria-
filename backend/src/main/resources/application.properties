# ===========================================
# Configuración Base - Spring Profiles
# ===========================================
# Por defecto usa perfil de desarrollo
# Para cambiar a producción: --spring.profiles.active=prod
spring.profiles.active=dev

# Configuración según perfil:
# - dev: application-dev.properties (tu máquina)
# - prod: application-prod.properties (servidor cliente)
