# ===========================================
# Configuración DESARROLLO (tu máquina)
# ===========================================
# Server Configuration
server.port=8080
spring.application.name=inmobiliario-backend

# MySQL Database Configuration - DESARROLLO
spring.datasource.url=jdbc:mysql://localhost:3306/sisarovi_dbb?createDatabaseIfNotExist=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=admin123
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Multipart limits for proforma PDF uploads
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# Resend email configuration (password recovery)
resend.enabled=true
resend.api.key=${RESEND_API_KEY:re_PszeqDzt_9m3RGndvFvcTbSoGUBFTpNDs}
resend.from.email=${RESEND_FROM_EMAIL:onboarding@resend.dev}

# RENIEC habilitado nuevamente para usar el token real
reniec.enabled=true
